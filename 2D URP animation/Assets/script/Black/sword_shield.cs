using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sword_shield : MonoBehaviour
{
    int attack_count;
    int attack_max_count;

    // Start is called before the first frame update
    void Start()
    {
        attack_count = 0;
        attack_max_count = 3;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void attack1()
    {

    }
}
